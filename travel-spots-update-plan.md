# Travel Spots JSON 데이터 보강 계획

본 문서는 `lib/philgo_files/travel/travel_spots.json` 파일의 기존 여행 정보를 보강(수정, 업데이트)하는 상세 계획입니다.

---

## 📋 목차

1. [현황 분석](#1-현황-분석)
2. [CoT 분석 (Chain-of-Thought)](#2-cot-분석-chain-of-thought)
3. [ToT 분석 (Tree-of-Thought)](#3-tot-분석-tree-of-thought)
4. [데이터 품질 문제 유형](#4-데이터-품질-문제-유형)
5. [업데이트 전략](#5-업데이트-전략)
6. [실행 계획 (Phase별)](#6-실행-계획-phase별)
7. [스크립트 활용 가이드](#7-스크립트-활용-가이드)
8. [검증 체크리스트](#8-검증-체크리스트)
9. [병렬 처리 시스템](#9-병렬-처리-시스템) ⭐ NEW

---

## 1. 현황 분석

### 1.1 데이터 규모

| 항목 | 수치 |
|------|------|
| 현재 여행지 총 개수 | 1,045개 |
| JSON 파일 위치 | `lib/philgo_files/travel/travel_spots.json` |

### 1.2 JSON 데이터 구조

```json
{
    "name": "여행지 한글 이름",
    "english name": "English Name",
    "title": "짧은 타이틀",
    "description": "상세 설명 (2-3문장)",
    "city": "도시명",
    "province": "주(Province)명",
    "icon": "이모지 아이콘",
    "category": "카테고리",
    "imageUrl": "대표 이미지 URL",
    "texts": [
        "# 마크다운 형식의 상세 정보 1",
        "## 방문 정보",
        "## 추천 포인트 & 여행 팁"
    ]
}
```

### 1.3 기존 작업 이력

- **여행지 추가 완료**: 677개 → 1,045개 (368개 추가)
- **이미지 URL 수정**: Wikipedia API 기반 이미지 URL 적용
- **기존 스크립트 위치**: `tmp/scripts/`, `lib/philgo_files/scripts/`

### 1.4 정보 검색 도구

Claude Code는 다음 도구들을 직접 사용하여 여행 정보를 검색하고 보강합니다:

#### WebSearch 도구
- **용도**: 인터넷에서 여행지 정보를 검색
- **활용 예시**:
  - `"Kawasan Falls Cebu opening hours 2026"` - 운영 시간 검색
  - `"El Nido entrance fee"` - 입장료 정보 검색
  - `"Chocolate Hills history"` - 역사적 배경 검색
- **장점**: 최신 정보, 다양한 출처에서 종합적인 정보 수집

#### WebFetch 도구
- **용도**: 특정 웹페이지에서 상세 정보 추출
- **활용 예시**:
  - 필리핀 관광청(DOT) 공식 사이트에서 정확한 정보 수집
  - TripAdvisor, Lonely Planet 등에서 여행 팁 추출
  - Wikipedia에서 역사, 지리, 문화 정보 수집
- **장점**: 특정 페이지의 심층 정보 획득, 정확한 데이터 검증

#### 정보 보강 전략
```
1단계: 기존 데이터 분석
   └─> 부족한 필드 식별 (texts 비어있음, description 부실 등)

2단계: WebSearch로 정보 검색
   └─> 여행지명 + 필요한 정보 유형으로 검색
   └─> 예: "Tubbataha Reef diving information"

3단계: WebFetch로 상세 정보 추출
   └─> 신뢰할 수 있는 출처에서 상세 정보 수집
   └─> 여러 출처 교차 검증으로 정확성 확보

4단계: 정보 가공 및 업데이트
   └─> 수집된 정보를 마크다운 형식으로 정리
   └─> JSON 필드에 맞게 구조화하여 저장
```

---

## 2. CoT 분석 (Chain-of-Thought)

### 2.1 문제 정의

> **핵심 질문**: 기존 1,045개 여행지 데이터 중 품질이 부실한 항목을 어떻게 식별하고 보강할 것인가?

### 2.2 단계별 사고 흐름

```
Step 1: 데이터 품질 기준 정의
  └─> 어떤 필드가 필수인가?
  └─> 각 필드의 품질 기준은?

Step 2: 현재 데이터 품질 분석
  └─> 누락된 필드가 있는 항목은 몇 개인가?
  └─> 부실한 내용을 가진 항목은 몇 개인가?

Step 3: 우선순위 결정
  └─> 어떤 항목을 먼저 보강해야 하는가?
  └─> 인기 지역 vs 희소 지역 중 우선순위는?

Step 4: 보강 방법 결정
  └─> 자동화 가능한 작업은?
  └─> 수동 검토가 필요한 작업은?

Step 5: 실행 및 검증
  └─> 보강 작업 실행
  └─> 품질 검증 및 피드백
```

### 2.3 필수 필드 정의

| 필드 | 필수 여부 | 품질 기준 |
|------|----------|----------|
| `name` | ✅ 필수 | 한글로 작성, 비어있지 않음 |
| `english name` | ✅ 필수 | 영문 이름 존재 |
| `title` | ✅ 필수 | 10~30자 내외의 매력적인 문구 |
| `description` | ✅ 필수 | 50자 이상의 설명 |
| `city` | ✅ 필수 | 도시명 존재 |
| `province` | ✅ 필수 | 주(Province)명 존재 |
| `icon` | ⚪ 선택 | 적절한 이모지 |
| `category` | ✅ 필수 | 카테고리 분류 |
| `imageUrl` | ✅ 필수 | HTTP 200 응답, 이미지 Content-Type |
| `texts` | ⚪ 선택 | 최소 3개 이상의 마크다운 콘텐츠 |

---

## 3. ToT 분석 (Tree-of-Thought)

### 3.1 문제 분해 (Branching)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    여행지 데이터 보강 문제 (Root)                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
        ┌────────────────────────────┼────────────────────────────┐
        ▼                            ▼                            ▼
┌───────────────────┐      ┌───────────────────┐      ┌───────────────────┐
│   Sub-Problem 1   │      │   Sub-Problem 2   │      │   Sub-Problem 3   │
│   이미지 URL 품질  │      │   텍스트 콘텐츠    │      │   메타데이터 품질  │
└───────────────────┘      └───────────────────┘      └───────────────────┘
        │                            │                            │
   ┌────┴────┐                  ┌────┴────┐                  ┌────┴────┐
   ▼         ▼                  ▼         ▼                  ▼         ▼
┌───────┐ ┌───────┐       ┌───────┐ ┌───────┐       ┌───────┐ ┌───────┐
│URL    │ │URL    │       │texts  │ │설명   │       │city   │ │영문명 │
│누락   │ │무효   │       │비어있음│ │부실   │       │누락   │ │누락   │
└───────┘ └───────┘       └───────┘ └───────┘       └───────┘ └───────┘
```

### 3.2 Sub-Problem 1: 이미지 URL 품질

**문제**: 이미지 URL이 누락되었거나 무효한 항목 존재

**해결 전략**:
1. `check_image_urls.dart` 스크립트로 모든 URL 검증
2. HTTP 404, 403 오류 URL 식별
3. Wikipedia API로 새 이미지 URL 검색
4. 기본 이미지 URL로 대체 (검색 실패 시)

**관련 스크립트**:
- `lib/philgo_files/scripts/check_image_urls.dart` - URL 검증
- `tmp/scripts/verify_all_urls_v2.dart` - Rate Limiting 대응 검증
- `lib/philgo_files/scripts/update_image_url.dart` - URL 업데이트

### 3.3 Sub-Problem 2: 텍스트 콘텐츠 품질

**문제**: `texts` 배열이 비어있거나 내용이 부실한 항목 존재

**해결 전략**:
1. `texts` 배열이 비어있는 항목 추출
2. **⭐ WebSearch 도구로 여행지 정보 검색**
   - 여행지명 + "travel guide", "tourist information" 등으로 검색
3. **⭐ WebFetch 도구로 상세 정보 수집**
   - 검색 결과의 신뢰할 수 있는 페이지에서 상세 정보 추출
4. 수집된 정보를 마크다운 형식으로 정리하여 업데이트

**texts 배열 구조 가이드**:
```markdown
texts[0]: # 여행지 소개 (제목, 아이콘, 핵심 특징)
texts[1]: ## 상세 설명 (역사, 특징, 액티비티)
texts[2]: ## 방문 정보 (위치, 운영시간, 입장료, 교통편)
texts[3]: ## 추천 포인트 & 여행 팁
```

### 3.4 Sub-Problem 3: 메타데이터 품질

**문제**: `city`, `province`, `english name` 등 메타데이터 누락

**해결 전략**:
1. 누락된 필드가 있는 항목 추출
2. **⭐ WebSearch 도구로 정확한 위치 정보 검색**
   - 여행지명 + "location", "where is" 등으로 검색
3. **⭐ WebFetch 도구로 공식 정보 확인**
   - Wikipedia, Google Maps 관련 정보에서 정확한 city/province 확인
4. 데이터 업데이트

---

## 4. 데이터 품질 문제 유형

### 4.1 유형별 분류

| 유형 | 설명 | 우선순위 | 자동화 가능 |
|------|------|----------|------------|
| Type A | imageUrl 누락/무효 | 🔴 높음 | ✅ 자동화 |
| Type B | texts 배열 비어있음 | 🔴 높음 | ⚪ 반자동 |
| Type C | description 부실 (50자 미만) | 🟡 중간 | ⚪ 반자동 |
| Type D | city/province 누락 | 🟡 중간 | ⚪ 반자동 |
| Type E | english name 누락 | 🟢 낮음 | ⚪ 반자동 |
| Type F | title 부실 (10자 미만) | 🟢 낮음 | ⚪ 반자동 |

### 4.2 품질 분석 스크립트 출력 예시

```
======================================
📊 Travel Spots 데이터 품질 분석 결과
======================================
총 항목 수: 1,045개

[품질 문제 요약]
  Type A (imageUrl 무효): 23개 (2.2%)
  Type B (texts 비어있음): 368개 (35.2%)
  Type C (description 부실): 45개 (4.3%)
  Type D (city/province 누락): 12개 (1.1%)
  Type E (english name 누락): 8개 (0.8%)
  Type F (title 부실): 15개 (1.4%)

[완전한 데이터]: 612개 (58.6%)
[보강 필요]: 433개 (41.4%)
======================================
```

---

## 5. 업데이트 전략

### 5.1 우선순위 기반 접근

```
Phase 1: 데이터 품질 분석 (스크립트 실행)
    │
    ▼
Phase 2: Type A 해결 - 이미지 URL 수정 (자동화)
    │
    ▼
Phase 3: Type B 해결 - texts 콘텐츠 보강 (반자동)
    │
    ▼
Phase 4: Type C~F 해결 - 메타데이터 보강
    │
    ▼
Phase 5: 최종 검증 및 품질 확인
```

### 5.2 지역별 처리 전략

인기 지역을 우선적으로 처리:

| 우선순위 | 지역 | 예상 항목 수 |
|---------|------|-------------|
| 1 | 팔라완 (Palawan) | ~150개 |
| 2 | 세부 (Cebu) | ~120개 |
| 3 | 보홀 (Bohol) | ~80개 |
| 4 | 보라카이/아클란 | ~60개 |
| 5 | 기타 비사야스 | ~200개 |
| 6 | 루손 지역 | ~300개 |
| 7 | 기타 | ~135개 |

---

## 6. 실행 계획 (Phase별)

### Phase 1: 데이터 품질 분석

**목표**: 현재 데이터의 품질 문제 파악

**실행 명령**:
```bash
# 1. 전체 항목 수 확인
dart run lib/philgo_files/scripts/list_travel_spots_names.dart

# 2. 이미지 URL 검증
dart run lib/philgo_files/scripts/check_image_urls.dart

# 3. 품질 분석 (새 스크립트)
dart run lib/philgo_files/scripts/analyze_data_quality.dart
```

**출력 파일**:
- `lib/philgo_files/scripts/failed_image_urls.json` - 실패한 URL 목록
- `lib/philgo_files/scripts/quality_analysis.json` - 품질 분석 결과

---

### Phase 2: 이미지 URL 수정

**목표**: 무효한 이미지 URL 수정

**방법**:
1. 실패한 URL 목록 확인
2. Wikipedia API로 새 이미지 검색
3. 이미지 URL 업데이트

**실행 명령**:
```bash
# 1. 실패한 URL 목록 확인
cat lib/philgo_files/scripts/failed_image_urls.json

# 2. 이미지 URL 수정 (새 스크립트)
dart run lib/philgo_files/scripts/fix_image_urls.dart
```

**Wikipedia API 사용**:
```
https://en.wikipedia.org/w/api.php?action=query&titles={검색어}&prop=pageimages&format=json&pithumbsize=800
```

---

### Phase 3: texts 콘텐츠 보강

**목표**: 비어있는 texts 배열 채우기

**방법**:
1. texts가 비어있는 항목 추출
2. **⭐ WebSearch 도구로 여행지 정보 검색**
   - 검색 예시: `"여행지명 travel guide"`, `"여행지명 things to do"`
3. **⭐ WebFetch 도구로 상세 정보 추출**
   - 검색 결과 중 신뢰할 수 있는 페이지에서 심층 정보 수집
   - 관광청, Wikipedia, TripAdvisor 등 활용
4. 수집된 정보를 마크다운 형식으로 texts 작성
5. JSON 업데이트

**실행 명령**:
```bash
# 1. texts 비어있는 항목 추출
dart run lib/philgo_files/scripts/extract_empty_texts.dart

# 2. AI 기반 콘텐츠 생성 (Gemini API 사용)
dart run lib/philgo_files/scripts/enhance_texts.dart --province "Palawan"
```

**texts 작성 템플릿**:

```markdown
# texts[0] - 여행지 소개
# {icon} {name} ({english name})

## 📌 한눈에 보기
- 🏷️ 한 줄 소개: {title}
- 🧭 분류: {category}
- 📍 위치: {city}, {province}

---

# texts[1] - 상세 설명
# 🌟 {name} ({english name})

{detailed_description}

## 특징
- 특징 1
- 특징 2
- 특징 3

---

# texts[2] - 방문 정보
## 🗺️ 방문 정보

### 위치
- **주소**: {address}
- **거리**: {distance}

### 운영 시간
- **영업시간**: {hours}

### 입장료
- **일반**: {fee}

### 교통편
- {transportation}

---

# texts[3] - 추천 포인트 & 여행 팁
## 💡 추천 포인트 & 여행 팁

### 추천 포인트
1. ...
2. ...

### 여행 팁
- ...
- ...
```

---

### Phase 4: 메타데이터 보강

**목표**: 누락된 메타데이터 채우기

**실행 명령**:
```bash
# 1. 누락된 메타데이터 항목 추출
dart run lib/philgo_files/scripts/extract_incomplete_metadata.dart

# 2. 메타데이터 업데이트
dart run lib/philgo_files/scripts/update_metadata.dart
```

---

### Phase 5: 최종 검증

**목표**: 모든 데이터 품질 확인

**실행 명령**:
```bash
# 1. 전체 이미지 URL 재검증
dart run lib/philgo_files/scripts/check_image_urls.dart

# 2. 품질 분석 재실행
dart run lib/philgo_files/scripts/analyze_data_quality.dart

# 3. JSON 유효성 검증
dart run lib/philgo_files/scripts/validate_json.dart
```

---

## 7. 스크립트 활용 가이드

### 7.1 기존 스크립트

| 스크립트 | 위치 | 기능 |
|---------|------|------|
| `list_travel_spots_names.dart` | `lib/philgo_files/scripts/` | 여행지 이름 목록 출력 |
| `check_image_urls.dart` | `lib/philgo_files/scripts/` | 이미지 URL 검증 |
| `update_image_url.dart` | `lib/philgo_files/scripts/` | 이미지 URL 업데이트 |

### 7.2 신규 작성 필요 스크립트

| 스크립트 | 위치 | 기능 |
|---------|------|------|
| `analyze_data_quality.dart` | `lib/philgo_files/scripts/` | 전체 데이터 품질 분석 |
| `extract_empty_texts.dart` | `lib/philgo_files/scripts/` | texts 비어있는 항목 추출 |
| `enhance_texts.dart` | `lib/philgo_files/scripts/` | AI 기반 texts 콘텐츠 생성 |
| `extract_incomplete_metadata.dart` | `lib/philgo_files/scripts/` | 메타데이터 누락 항목 추출 |
| `update_metadata.dart` | `lib/philgo_files/scripts/` | 메타데이터 업데이트 |
| `validate_json.dart` | `lib/philgo_files/scripts/` | JSON 유효성 검증 |
| `fix_image_urls.dart` | `lib/philgo_files/scripts/` | 무효 이미지 URL 자동 수정 |

---

## 8. 검증 체크리스트

### Phase 1 완료 후
- [ ] 품질 분석 결과 파일 생성 확인
- [ ] 문제 유형별 항목 수 파악

### Phase 2 완료 후
- [ ] 모든 이미지 URL HTTP 200 응답
- [ ] Content-Type이 image/* 확인

### Phase 3 완료 후
- [ ] texts 배열이 비어있는 항목 0개
- [ ] 각 texts 항목에 마크다운 콘텐츠 존재

### Phase 4 완료 후
- [ ] city 누락 항목 0개
- [ ] province 누락 항목 0개
- [ ] english name 누락 항목 0개

### Phase 5 완료 후
- [ ] 전체 JSON 유효성 검증 통과
- [ ] 품질 분석 결과 "완전한 데이터" 비율 95% 이상
- [ ] 앱에서 데이터 로딩 테스트 완료

---

## 부록: 작업 명령어 Quick Reference

```bash
# 현재 여행지 수 확인
dart run lib/philgo_files/scripts/list_travel_spots_names.dart | tail -1

# 이미지 URL 검증
dart run lib/philgo_files/scripts/check_image_urls.dart

# 품질 분석
dart run lib/philgo_files/scripts/analyze_data_quality.dart

# texts 비어있는 항목 추출
dart run lib/philgo_files/scripts/extract_empty_texts.dart

# 이미지 URL 자동 수정
dart run lib/philgo_files/scripts/fix_image_urls.dart

# 최종 검증
dart run lib/philgo_files/scripts/validate_json.dart
```

---

## 9. 병렬 처리 시스템

### 9.1 개요

대량의 여행 정보(1,045개)를 효율적으로 처리하기 위해 **병렬 처리 시스템**을 도입했습니다.
최대 10개의 Worker를 동시에 실행하여 처리 속도를 크게 향상시킬 수 있습니다.

**핵심 특징**:
- 🔒 **Lock 메커니즘**: 파일 기반 Mutual Exclusion으로 중복 처리 방지
- 📊 **실시간 모니터링**: 진행 상황을 실시간으로 확인
- 🔄 **재시도 지원**: 실패한 항목 재처리 가능
- 📁 **상태 영속성**: 작업 중단 후에도 이어서 작업 가능

### 9.2 폴더 구조

**스크립트 위치**: `lib/philgo_files/scripts/`
**작업 데이터 위치**: `tmp/travel-spots-updating/`

```
lib/philgo_files/scripts/
├── parallel_lock_utils.dart # Lock 유틸리티 라이브러리
├── parallel_worker.dart     # 병렬 Worker 스크립트
├── parallel_monitor.dart    # 모니터링 스크립트
└── launch_workers.sh        # Worker 일괄 실행 스크립트

tmp/travel-spots-updating/
├── locks/                   # 현재 처리 중인 항목 lock 파일
│   ├── 0.lock              # 인덱스 0번 항목 처리 중
│   ├── 5.lock              # 인덱스 5번 항목 처리 중
│   └── ...
├── completed/              # 완료된 항목 기록
│   ├── 0.done             # 인덱스 0번 항목 완료
│   ├── 3.done             # 인덱스 3번 항목 완료
│   └── ...
├── failed/                 # 실패한 항목 기록
│   ├── 2.failed           # 인덱스 2번 항목 실패 (사유 포함)
│   └── ...
├── logs/                   # Worker별 로그
│   ├── worker_1.log
│   ├── worker_2.log
│   └── ...
└── progress.json           # 전체 진행 상황 요약
```

### 9.3 Lock 메커니즘 상세

```
┌─────────────────────────────────────────────────────────────────┐
│                    병렬 처리 Lock 흐름도                          │
└─────────────────────────────────────────────────────────────────┘

Worker 1                     Worker 2                     Worker 3
   │                            │                            │
   ▼                            ▼                            ▼
┌──────────┐               ┌──────────┐               ┌──────────┐
│ 항목 선택 │               │ 항목 선택 │               │ 항목 선택 │
│ (index=5)│               │ (index=5)│               │ (index=7)│
└────┬─────┘               └────┬─────┘               └────┬─────┘
     │                          │                          │
     ▼                          ▼                          ▼
┌──────────┐               ┌──────────┐               ┌──────────┐
│Lock 확인 │               │Lock 확인 │               │Lock 확인 │
│ 5.lock?  │               │ 5.lock?  │               │ 7.lock?  │
└────┬─────┘               └────┬─────┘               └────┬─────┘
     │ 없음                     │ 있음!                    │ 없음
     ▼                          ▼                          ▼
┌──────────┐               ┌──────────┐               ┌──────────┐
│Lock 생성 │               │  건너뜀  │               │Lock 생성 │
│ 5.lock   │               │ 다음 선택│               │ 7.lock   │
└────┬─────┘               └──────────┘               └────┬─────┘
     │                                                     │
     ▼                                                     ▼
┌──────────┐                                         ┌──────────┐
│ 처리 수행│                                         │ 처리 수행│
│ index=5  │                                         │ index=7  │
└────┬─────┘                                         └────┬─────┘
     │                                                     │
     ▼                                                     ▼
┌──────────┐                                         ┌──────────┐
│Lock 해제 │                                         │Lock 해제 │
│ 5.done   │                                         │ 7.done   │
└──────────┘                                         └──────────┘
```

**Lock 상태 종류**:

| 상태 | 파일 | 의미 |
|------|------|------|
| `available` | 없음 | 처리 가능 |
| `locked` | `{index}.lock` | 다른 Worker가 처리 중 |
| `completed` | `{index}.done` | 이미 완료됨 |
| `failed` | `{index}.failed` | 이전에 실패함 |

**Lock 파일 내용 예시** (`locks/5.lock`):
```json
{
    "index": 5,
    "workerId": "worker_1",
    "lockedAt": "2026-01-19T10:30:00.000",
    "spotName": "El Nido"
}
```

### 9.4 Worker 실행 방법

#### 방법 1: 개별 Worker 실행 (권장)

여러 터미널에서 각각 Worker를 실행합니다.

```bash
# 터미널 1
dart run lib/philgo_files/scripts/parallel_worker.dart --worker-id 1

# 터미널 2
dart run lib/philgo_files/scripts/parallel_worker.dart --worker-id 2

# 터미널 3
dart run lib/philgo_files/scripts/parallel_worker.dart --worker-id 3

# ... 최대 10개까지
```

#### 방법 2: 일괄 실행 스크립트

```bash
# 5개 Worker 동시 실행
./lib/philgo_files/scripts/launch_workers.sh 5

# 10개 Worker로 Palawan 지역만 처리
./lib/philgo_files/scripts/launch_workers.sh 10 --province Palawan

# 3개 Worker로 dry-run 테스트
./lib/philgo_files/scripts/launch_workers.sh 3 --dry-run
```

#### Worker 옵션

| 옵션 | 설명 | 예시 |
|------|------|------|
| `--worker-id` | Worker 식별자 (필수) | `--worker-id 1` |
| `--province` | 특정 지역만 처리 | `--province Palawan` |
| `--type` | 처리 유형 필터 | `--type texts` |
| `--max-items` | 최대 처리 항목 수 | `--max-items 100` |
| `--dry-run` | 미리보기 모드 | `--dry-run` |

### 9.5 모니터링

#### 진행 상황 조회

```bash
# 현재 진행 상황 확인
dart run lib/philgo_files/scripts/parallel_monitor.dart

# 상세 정보 (지역별 통계 포함)
dart run lib/philgo_files/scripts/parallel_monitor.dart --verbose

# 실시간 모니터링 (5초마다 갱신)
dart run lib/philgo_files/scripts/parallel_monitor.dart --watch
```

#### 모니터링 출력 예시

```
======================================================================
📊 Travel Spots 병렬 처리 모니터
======================================================================
시간: 2026-01-19 10:35:22

📈 전체 진행 상황
----------------------------------------
  총 항목: 1045개
  ✅ 완료: 423개 (40.5%)
  ❌ 실패: 12개 (1.1%)
  🔄 처리 중: 8개
  ⏳ 대기 중: 602개

  [████████████████░░░░░░░░░░░░░░░░░░░░░░░░] 40.5%

🔄 현재 처리 중인 항목
----------------------------------------
  [156] El Nido Beach - worker_1 (12초 경과)
  [289] Chocolate Hills - worker_2 (8초 경과)
  [445] Kawasan Falls - worker_3 (5초 경과)
  ...

👥 Worker 활동 상태
----------------------------------------
  worker_1: 🟢 활성 (3초 전)
  worker_2: 🟢 활성 (5초 전)
  worker_3: 🟢 활성 (2초 전)
  worker_4: 🟡 대기 (45초 전)
======================================================================
```

### 9.6 관리 명령어

```bash
# 모든 lock 초기화 (진행 중인 작업 취소)
dart run lib/philgo_files/scripts/parallel_monitor.dart --clear-locks

# 실패한 항목만 초기화 (재시도 가능하게)
dart run lib/philgo_files/scripts/parallel_monitor.dart --retry-failed

# 모든 진행 상황 초기화 (완전히 처음부터)
dart run lib/philgo_files/scripts/parallel_monitor.dart --reset

# 실행 중인 모든 Worker 종료
pkill -f parallel_worker.dart
```

### 9.7 병렬 처리 워크플로우

```
┌─────────────────────────────────────────────────────────────────┐
│                    병렬 처리 전체 워크플로우                       │
└─────────────────────────────────────────────────────────────────┘

Step 1: 준비
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 데이터 품질 분석
  dart run lib/philgo_files/scripts/analyze_data_quality.dart

  # 현재 진행 상황 초기화 (필요시)
  dart run lib/philgo_files/scripts/parallel_monitor.dart --reset

Step 2: 병렬 처리 실행
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 터미널 1: 모니터링
  dart run lib/philgo_files/scripts/parallel_monitor.dart --watch

  # 터미널 2~11: Worker 실행 (최대 10개)
  dart run lib/philgo_files/scripts/parallel_worker.dart --worker-id 1
  dart run lib/philgo_files/scripts/parallel_worker.dart --worker-id 2
  ...

Step 3: 완료 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 최종 진행 상황 확인
  dart run lib/philgo_files/scripts/parallel_monitor.dart --verbose

  # 실패 항목 재시도 (필요시)
  dart run lib/philgo_files/scripts/parallel_monitor.dart --retry-failed
  dart run lib/philgo_files/scripts/parallel_worker.dart --worker-id 1

Step 4: 검증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # JSON 유효성 검증
  dart run lib/philgo_files/scripts/validate_json.dart

  # 품질 분석 재실행
  dart run lib/philgo_files/scripts/analyze_data_quality.dart
```

### 9.8 주의사항

1. **동시 실행 제한**: 최대 10개의 Worker 동시 실행 권장
   - API Rate Limiting 고려
   - 시스템 리소스 고려

2. **Lock 타임아웃**: 5분 이상 경과된 lock은 자동 해제됨
   - Worker 비정상 종료 시 자동 복구

3. **JSON 파일 접근**: 각 Worker는 메모리에서 JSON을 수정하고 개별적으로 저장
   - 최종 결과는 마지막 Worker의 저장 결과
   - 중요: 현재 구현은 dry-run 모드만 안전

4. **재시작 시 동작**: 완료/실패 기록은 영속적으로 유지됨
   - 이어서 작업 가능
   - `--reset` 옵션으로 초기화 가능

---

## 부록: 작업 명령어 Quick Reference

```bash
# 현재 여행지 수 확인
dart run lib/philgo_files/scripts/list_travel_spots_names.dart | tail -1

# 이미지 URL 검증
dart run lib/philgo_files/scripts/check_image_urls.dart

# 품질 분석
dart run lib/philgo_files/scripts/analyze_data_quality.dart

# texts 비어있는 항목 추출
dart run lib/philgo_files/scripts/extract_empty_texts.dart

# 이미지 URL 자동 수정
dart run lib/philgo_files/scripts/fix_image_urls.dart

# 최종 검증
dart run lib/philgo_files/scripts/validate_json.dart

# === 병렬 처리 명령어 ===

# Worker 실행 (개별)
dart run lib/philgo_files/scripts/parallel_worker.dart --worker-id 1

# Worker 일괄 실행
./lib/philgo_files/scripts/launch_workers.sh 5

# 진행 상황 모니터링
dart run lib/philgo_files/scripts/parallel_monitor.dart --watch

# 실패 항목 재시도
dart run lib/philgo_files/scripts/parallel_monitor.dart --retry-failed

# 모든 Worker 종료
pkill -f parallel_worker.dart
```

---

*작성일: 2026-01-18*
*업데이트: 2026-01-19 (병렬 처리 시스템 추가)*
*버전: 1.1*
